# Pinned top-level ML runtime packages for the managed uv environment.
#
# vLLM 0.16.0 requires the PyTorch 2.9.1 stack. Keep these pins aligned when
# updating the environment so ad-hoc PyTorch work and the OpenAI-compatible
# server use the same versions. Experimental nightly GLM setups live in the
# separate vllm-nightly requirements file.
vllm==0.16.0
torch==2.9.1
torchaudio==2.9.1
torchvision==0.24.1
